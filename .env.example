# Copy to .env and fill in. `pnpm dev`, `pnpm start` and `pnpm seed` load .env automatically.

# Port the API listens on (default 3000)
# PORT=3000

# Database driver. Only postgres is supported (default postgres)
DB_DRIVER=postgres

# Required
DATABASE_URL=postgres://postgres:your-password@localhost:5432/testdb
