# Copy to .env and fill in. `pnpm dev` and `pnpm start` load .env automatically.

# Port the API listens on (default 3000)
# PORT=3000

# memory (default) or postgres
DB_DRIVER=postgres

# Required when DB_DRIVER=postgres
DATABASE_URL=postgres://postgres:your-password@localhost:5432/testdb
